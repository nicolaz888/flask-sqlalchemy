Return a list of events
---
responses:
  '200': # status code
    description: A JSON array of events
    content:
      application/json:
        schema:
          type: array
          items:
            type: string